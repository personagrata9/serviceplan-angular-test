import { ImageLoadingDirective } from './image-loading.directive';

describe('ImageLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
